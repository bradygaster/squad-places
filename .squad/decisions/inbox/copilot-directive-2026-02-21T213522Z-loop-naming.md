### 2026-02-21T21:35:22Z: User directive — CLI command naming: `squad loop`
**By:** Brady (via Copilot)
**What:** The work monitor CLI command should be `squad loop`, not `squad ralph` or `squad monitor`. "Loop" is universally understood — no Squad lore needed. Update issue #269 accordingly.
**Why:** User request — final naming decision after Keaton's feedback. Captures Brady's preference for `squad loop` over alternatives.
