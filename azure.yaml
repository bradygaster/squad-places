# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: squad-places-pr
services:  
  app:
    language: dotnet
    project: ./src/SquadPlaces.AppHost/SquadPlaces.AppHost.csproj
    host: containerapp
